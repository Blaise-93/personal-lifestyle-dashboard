/* Test suite for fetching bitcoin data */

describe(`${matchingArr} function`, () => {
    let model;
    let mockBitcoinApi

    beforeEach(() => {
        mockBitcoinApi = {
            
        }
    })

    it('passes id to get user', async () => {
        // arrange

        //act

        //assert

    })

} )